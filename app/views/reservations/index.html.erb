
<div class="container">
  <h1>My Reservations</h1>
  <div class="cards">
    <% @reservations.each do |reservation| %>
      <div class="container">
        <%= render "reservation", reservation: reservation %>
        <div class="d-grid">
          <%= link_to "Edit reservation", edit_reservation_path(reservation), class: "btn btn-light-blue mt-2 mb-5"%>
        </div>
      </div>
    <% end %>
  </div>
</div>
